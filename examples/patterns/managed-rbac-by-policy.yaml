apiVersion: v1
kind: Namespace
metadata:
  name: workflow-ops
  labels:
    platform: workflow
    env: production
---
apiVersion: rbac.therapist.io/v1alpha1
kind: Team
metadata:
  name: care-circle-operators
spec:
  description: Team operating workflow infrastructure.
  members:
    - kind: Group
      name: workflow-operators@acme.com
---
apiVersion: rbac.therapist.io/v1alpha1
kind: AccessPolicy
metadata:
  name: healthy-boundary-contracts
spec:
  rationale: >
    AccessPolicy manages custom Role/ClusterRole resources and binds them to
    workflow operators from a single declarative object.
  subjects:
    - teamRef:
        name: care-circle-operators
  managedClusterRoles:
    - name: boundary-template-reader
      rules:
        - apiGroups: ["argoproj.io"]
          resources: ["workflows", "workflowtemplates"]
          verbs: ["get", "list", "watch"]
  managedRoles:
    - name: boundary-secret-reader
      namespace: workflow-ops
      rules:
        - apiGroups: [""]
          resources: ["secrets"]
          verbs: ["get", "list"]
  roles:
    - clusterRole: boundary-template-reader
      namespaces:
        names:
          - workflow-ops
    - role: boundary-secret-reader
      namespaces:
        names:
          - workflow-ops
